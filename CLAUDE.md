# CLAUDE.md — 오케스트레이터 컨텍스트

## 프로젝트 개요

2026 금융 AI Challenge(주최: 금융보안원, 후원: 금융위원회) 참가 프로젝트.
금융 현안을 정의하고 AI 기반 해결 웹서비스를 MVP로 구현해 제출한다.

- **참가 형태**: 개인 참가 (사용자 1인 + AI 에이전트)
- **1차 마감**: 2026. 9. 7.(월) 10:00 — 기획서 PDF + 기능명세서 PDF + 배포 URL
- **URL 유지**: 9. 7. 11:00 ~ 9. 11. 23:59 접근 불가 시 결격
- 전체 요강: `docs/competition/overview.md` / 제출 체크리스트: `docs/competition/checklist.md`

## 현재 상태 (2026-07-27)

- 주제: **확정 — 「기록으로」 — 내 편의 사고조사실** (`docs/planning/31-solution-lock.md`)
  > 무단이체·명의도용·사기 피해자가, 은행이 **기억으로 답하라고 요구하는 항목**을 자기 기기에
  > 남아 있는 **기록으로** 답하고, **어느 제도의 문이 열려 있는지와 그 문들 사이의 순서**를
  > 신청 전에 확인하는 무설치·무로그인 웹 서비스.
- 기술 스택: **확정** — Next.js+TS / Vercel / Claude API / SSE / Neon Postgres / MCP SDK
  (`docs/planning/03-stack-proposal.md`)
- 스테이지: **P단계 종료(judge 93.8, 목표 85) → M단계 W1 착수.**
  기획서 `1.2`(`40-proposal-draft.md`) · 기능명세서 `1.0`(`41-spec-draft.md`) ·
  대체 경로 규칙 원본(`32-alt-route-track.md`)
- **⛔ 이전 컨셉 「골든타임」은 2026-07-26 폐기.** 문서는 `docs/archive/goldentime/`,
  전체 스냅샷은 git 태그 `goldentime-final`. **거기 수치·기능·계약을 인용하지 않는다.**
- 주요 결정은 `docs/decisions.md`에 기록할 것

### 문제 정의 (한 문장 — 기획서 §3.1)

> 한국의 배상 심사는 두 번 거른다. 신청의 **34.7%**는 「피해자가 직접 이체했다」는 이유로
> 심사에 들어가지도 못하고, 들어간 것 중 대부분이 「이용자 과실」로 끝난다.
> **두 관문의 규칙은 전부 공개돼 있는데, 그 규칙에 넣을 사실만 소비자 쪽에 없다.**

깔때기(5대 은행, 2024-01~2025-08, 이인영 의원실): 상담 **2,135** → 신청 **173**
→ 심사제외 **60(34.7%)** → 배상 **18** = **상담 대비 0.84%**. 최대 손실 구간은
「상담→신청」의 **91.9%**이며, 그 사유 분포는 자료가 없어 `[추론]`으로 표기한다.

### 범위 계약 (2026-07-27 — 코어 추가·축소 금지)

- **T1 코어(12)**: F-01~F-11, F-17 / **T2 신뢰·A면(5)**: F-12·F-13·F-15·F-16·F-19
- **T3**: F-18 / **T4**: F-14 MCP
- 일정 압박 시 **삭제가 아니라 축소**(명세서 §2.3의 축소판 계약)
- 주차 게이트 **W1~W6**(명세서 §9.1), 릴리스 게이트 10항목(§9.2)

### 문서 작업 원칙

- **수치 인용**: `19-verify-final3.md`·`04-verification-digest.md` 정정표 통과분만.
  **기간·주체·출처 병기 필수.** 「176건」은 오기 — 원본 **173건**
- **법령 인용은 시행령까지 대조**한다(예: 통신사기피해환급법 §5①6호 "3만원" ↔
  시행령 §6② **1만원**). **시행일 병기.** 원문은 **법제처 Open API**
  (`law.go.kr/DRF/lawService.do?OC=test&target=law&MST=<번호>&type=XML`)로 취득 —
  웹 페이지는 SPA라 본문이 안 열린다
- **금지 표현**: 피해 "폭증" / "최초·유일" / "무과실 배상 통과" / 특정 은행 배상 실적 지목 /
  기능 부재 단정(→"확인되지 않았다") / 확률·승산·예상 금액 / "AI가 판정한다·결정한다"
- **용어 계약**: `confidence_label`=`stated`·`recorded`·`absent`(확률 아님) /
  "예비 평가" / "행동 이벤트 이력(내 기기 보관)" / 금액은 **구간(band)만** /
  로맨스스캠의 통신사기피해환급법 §2②가목 해당 여부는 `[해석]` — 확정 판정 금지
- **개정 루프**: 리뷰 → 개정 계약 문서 확정 → 초안 반영(버전 올림) → judge 재채점 → 재검증.
  개정은 반드시 계약 문서를 먼저 쓰고 따른다

## 에이전트 운용 규칙

Claude Code가 오케스트레이터, Codex CLI가 하위 코딩 에이전트다.

- Claude 담당: 리서치, 기획, 아키텍처 결정, 작업 분해·위임, 코드 리뷰, 통합, 문서화
- Codex 담당: 명확히 스코프가 정의된 구현 작업 (컴포넌트 구현, 테스트 작성, 리팩터링 등)

### Codex 위임 방법

Bash로 비대화형 실행. **모델은 반드시 `gpt-5.6-sol` + `xhigh`를 명시한다** (사용자 지정 정책, 2026-07-24):

```bash
codex exec --skip-git-repo-check -m gpt-5.6-sol -c model_reasoning_effort=xhigh \
  -s workspace-write -C <작업디렉토리> "<작업 지시>"
```

- 웹 리서치가 필요한 작업엔 `-c tools.web_search=true -c sandbox_workspace_write.network_access=true` 추가.
- 긴 지시는 프롬프트 파일로 작성 후 `- < prompt.md` 로 stdin 전달.

- 작업 지시는 **자기완결적**으로 작성: 목표, 대상 파일, 제약, 완료 기준을 명시. Codex는 `AGENTS.md`를 자동으로 읽으므로 프로젝트 공통 컨텍스트는 거기에 유지한다.
- Codex 결과물은 반드시 Claude가 리뷰 후 통합한다 (diff 확인, 테스트 실행).
- 병렬 작업 시 파일 충돌이 없도록 작업 단위를 분리해 위임한다.

## 채점 루프 (judge 스킬)

- 스테이지 산출물을 커밋할 때마다 `judge` 스킬(`.claude/skills/judge/`)로 자가 채점한다.
  루브릭: `docs/judging/rubric.md` / 리포트: `docs/judging/scores/`
- 스테이지 목표: R 80 / P 85 / M 85 / F 90. 목표 미달이면 개선 백로그를 처리한 뒤 재채점.
- **`docs/archive/` 아래는 채점·인용 대상이 아니다.**
- git commit 실행 시 PostToolUse 훅(`.claude/settings.json`)이 채점 리마인드를 띄운다.

## Git 규칙

- 원격: `https://github.com/mamekuma-sys/finance-ai-challenge` (origin, 브랜치 `main`).
- **커밋 후 즉시 `git push origin main`** — 커밋만 하고 푸시를 미루지 않는다
  (다른 세션·기기에서 이어받는 워크플로우이므로 로컬 전용 커밋을 남기지 말 것).

## 작업 규칙

- 공모전 제출물(기획서, 기능명세서)은 데이콘 제공 양식 기반으로 작성 후 PDF 변환.
- 실제 금융데이터 사용 불가 — 공개 데이터(금융감독원 오픈API, 공공데이터포털 등) 또는 합성 데이터만 사용.
- 심사위원이 직접 URL에 접속해 사용한다 — 로그인 장벽 최소화, 데모 계정·샘플 데이터 필수.
- 타인 저작물·코드·데이터 무단 사용 금지 (표절 시 실격).

## 미해결 확인 과제 (웹 검색 예산 소진으로 보류)

1. **카카오뱅크 소비자보호 > 비대면 금융사고(FA) 페이지 본문** — SPA로 미취득.
   이미 앱에서 증적·심사 안내를 제공하면 **G-기존수단 판정이 바뀐다.** 우선순위 1
2. **로맨스스캠의 통신사기피해환급법 §2②가목 해당 여부** — 유권해석·판례 미확보
