# 채점 리포트 009 — 스테이지 M (2026-07-25)

## 총점: 63.5 / 100 (목표: 85) — **BELOW TARGET**

| 차원 | 점수 | 가중치 | 근거 요약 |
|---|---:|---:|---|
| R1 주제 적합성 | 6 | 15 | AI가 설계상 핵심 루프에 있으나 **현재 빌드에 AI 경로 0건** |
| R2 문제 정의 | 7 | 20 | 1차 출처 실접속 검증 통과분만 인용, 감점 트리거 0건. 다만 핵심 수요 근거가 미확보로 남음 |
| R3 솔루션 설계·AI 깊이 | 7 | 20 | 깊이 신호 D4·D5 실구현 + 안전성 장치 가점. D1·D2·D3 미구현 |
| R4 MVP 완성도·실현성 | 4 | 20 | **배포 URL 부재**, 런타임 코어 9 중 5개만 작동 |
| R5 독창성·차별성 | 8 | 10 | 비교표 존재, 💀 전제 미사용, "최초·유일" 0건, 차별점이 구현으로 뒷받침되기 시작 |
| R6 기대효과·시상 정합성 | 6 | 10 | 정량화가 r5 검증으로 약화. 정책 과제 연결은 유지 |
| R7 문서 품질·AI 판독성 | 9 | 5 | 계약 문서 체계(r1~r6), 용어 일관, 수치마다 출처 |

계산: (6×15 + 7×20 + 7×20 + 4×20 + 8×10 + 6×10 + 9×5) ÷ 100 × 10 = **63.5**

## 게이트

| 게이트 | 판정 | 근거 |
|---|---|---|
| G1 URL 생존 | **FAIL** | 배포 URL이 `README.md`·`docs/` 어디에도 없다. 로컬 `PORT=3737 npm run start`만 확인됨. 심사위원은 접속할 수 없다 |
| G2 양식 준수 | N.A. | 기획서·기능명세서 PDF 미작성(W6 범위). 원본 md는 존재 |
| G3 데이터 적법성 | **PASS** | 실금융데이터·개인정보 0건. 상호작용 중 네트워크 요청 0건을 CDP로 실측. `app/src/lib/decision/sources.ts`의 6→11개 출처 전부 공개 기관 자료 |
| G4 저작권 | **PASS** | `app/NOTICE.md:3` Pretendard 1.3.9 OFL-1.1 고지. 모든 인용에 출처 URL 병기 |
| G5 심사 접근성 | **PASS(조건부)** | 로그인·계정·토큰 없음(`app/src/app/page.tsx`). 단 G1 미충족이라 실질 접근 불가 |
| G6 제출물 완비 | **FAIL** | 기획서 PDF + 기능명세서 PDF + URL 3종 중 0종 완비 |

**G1·G6 FAIL은 현재 W1 시점의 정상 상태**(배포는 W6 범위)이나, 루브릭상 M 스테이지
게이트이므로 FAIL로 기록한다.

## 차원별 상세

### R1 주제 적합성 — 6/10 (가중치 15)

**근거:** 서비스 정의는 AI를 핵심에 둔다 — `docs/planning/11-spec-draft.md:20-22`
"AI 판정보다 먼저 공식 첫 행동을 제시하고 … 6단계 처리·검증 파이프라인". 금융소비자
특성·채널 반영도 명시적이다(§1.2 사용자 유형 5종, F-10 고령자 모드는 실제 작동 —
`app/src/components/rule0-desk.tsx` main 20px·기본 행동 48px 실측 확인).

**감점:** 현재 빌드에 **AI 호출 경로가 0건**이다. `grep -rl "VerdictLevel|evidence_strength"
app/src/components/` → 0건, F-03 판정·F-04 자유질문·6단계 파이프라인 미구현. 루브릭 R1의
0점 조건 "AI 없이 동일 가치 제공 가능"이 **현 시점 빌드에는 사실**이다. 결정 엔진은
의도적으로 결정적 규칙이며(§6.2에서 긴급 행동의 LLM 자유생성 금지) 이는 설계상 강점이지만,
심사위원이 지금 URL을 열면 AI를 볼 수 없다.

**올리려면:** W4의 LLM 3단계(트리아지·사건 분석·피해자 케어)를 최소 1단계라도 배포에
올려 AI 기여가 화면에서 보이게 한다. §5.4.3 KPI ⑤(규칙형 폼 대비 LLM 보조 비교)를
합성 사건으로 실행해 AI 기여를 정량 제시하면 8점 이상 가능.

### R2 문제 정의 — 7/10 (가중치 20)

**근거(가점):** 정량 근거를 1차 출처에 직접 접속해 검증하고 그 결과만 인용한다 —
`app/docs/evidence-verified.md`(243줄). 검증 결과 **계약이 인용하던 수치 6건 중 4건을
기각**했고, 기각분을 문서·화면에서 전량 제거했다(`grep -cE "620명|25.9%|1조 2,578억" ` →
0건). `docs/planning/10-proposal-draft.md` §3.2는 제거된 근거 자리에 **"시장 규모 근거
미확보", "사용자 인지 근거 미확보"** 행을 두고 미확보 사유를 명시한다 — 루브릭 R2의
감점 트리거(미검증 수치 인용 -2, 부재 단정 -2, 정부 프레임 배치 -2)가 **0건**이다.
법정 기한 오류("3영업일" → 시행령 제3조 "신청한 날부터 3일 이내")를 발견해 개정한 것은
전문가층 감점을 사전 차단한 조치다(`docs/planning/21-revision-plan-r5.md` H1).

**감점:** 검증의 대가로 **문제의 수요 근거가 얇아졌다.** "골든타임 도달 실패"를 뒷받침하던
금감원 620명 설문이 전면 금지되어, 지금 남은 정량 근거는 제도 사실(30분 지연인출제도)과
추세(35.3% 감소)뿐이다. "왜 지금인가"와 "기존 서비스가 이 문제를 못 푸는 이유의 입증"이
정량적으로 약하다. 루브릭 10점 조건의 "검증 통과한 정량 근거"는 충족하나 "구체적 타깃
페르소나 + 왜 지금인가"의 근거 강도가 부족하다.

**올리려면:** ① 금감원 설문 원문 보도자료 URL을 확보해 복원하거나, ② 대체 가능한 공식
1차 통계(경찰청 연도·정의 명시 통계 등)를 확보한다. ③ 미확보 상태를 유지한다면 MVP 안의
사용성 테스트(합성 과업 N≥10)로 자체 근거를 만들어 제시한다.

### R3 솔루션 설계·AI 깊이 — 7/10 (가중치 20)

**근거(깊이 신호, `docs/research/03-scoping-review.md` 기준):**
- **D4 도메인 계산 엔진 — 실구현.** 조치 결정 엔진이 6개 상태 필드의 **1,152개 전 조합**에서
  결정적 결과를 산출하는 총함수다(`app/src/lib/decision/engine.ts`). 전수 property 테스트
  4종 + §4.5.4 인수 스냅샷 5종이 통과하며, 병합 합집합 기대값을 엔진 출력이 아니라
  `RULES` 원본에서 **독립 재계산**해 대조한다(`engine.exhaustive.test.ts:94-132`).
- **D5 평가·안전 하네스 — 실구현.** ① 입력 fail-closed(`INVALID_INCIDENT_STATE`, 오류
  본문 입력 미반사) ② 템플릿 활성 게이트 3상태 + 날짜 주입형 resolver(§4.7.1)
  ③ **citation entailment 자동 검증** — 카드 동사↔지지 출처 매핑을 전 조합에서 대조
  (7,350개 카드 검사 위반 0건) ④ 금지 문자열 회귀 테스트 ⑤ 안전 기기 전제 교차 계층 검증.
- **D6 양면 시스템 — 부분.** 사람 UI + `/llms.txt` 기계 안내가 있으나 REST·MCP는 미구현.

**가점(+1):** 금융 AI 안전성 검증 장치를 직접 만들어 보여준다 — 금보원 2026 전략 정합.
특히 entailment 자동 검증과 템플릿 fail-closed 게이트는 루브릭이 요구하는
"AI 출력의 근거 인용 강제·가드레일"의 결정적 계층 구현이다.

**감점:** 루브릭 10점 조건은 깊이 신호 **3개 이상 실구현**이다. D1(멀티에이전트)·
D2(실시간·시뮬레이션)·D3(비정형 파이프라인)은 전부 미구현이며, 이들이 AI 깊이의 핵심이다.
현재는 "도메인 규칙 엔진 + 안전 하네스"로, AI 깊이가 아니라 **소프트웨어 공학 깊이**다.

**올리려면:** W4의 6단계 파이프라인(LLM 3단계) 구현 시 D3가 채워지고, SSE 개인 타임라인
(F-13)이 D2를 채운다. 두 개만 올라와도 3개 요건을 충족해 9점권.

### R4 MVP 완성도·실현성 — 4/10 (가중치 20)

**근거(가점):** 작동하는 부분의 검증 강도는 높다. 프로덕션 빌드를 실제로 띄우고 헤드리스
Chrome CDP로 실측한 결과 — 가로 오버플로 **0**(360/390/768/1280/1440 × 라이트·다크 10개
조합), **Rule 0 p95 26.9ms**(NF-06 상한 2,000ms), 상호작용 중 네트워크 요청 **0건**
(NF-09 무전송 실증), 콘솔 에러 **0건**, `/`·`/llms.txt` 200. 테스트 **83개** 통과.
합성·공개 데이터임을 UI에 명시한다(`구조화 상태 선택 데모 · 무로그인 · 서버 무저장`).

**감점(치명):**
- **배포 URL이 없다.** 루브릭 R4 10점 조건의 첫 항목 "URL 즉시 접속"이 불가능하다.
  심사위원은 이 MVP를 볼 수 없다.
- **런타임 코어 9 중 4개 미구현.** `app/src/app/`에는 `/`와 `/llms.txt`뿐이다.
  F-01(합성 샘플·`scenario_id`) 0건, F-03(판정) 0건, F-04(LLM 확인 질문) 미구현,
  F-08(브리핑 PDF) 0건. `docs/planning/11-spec-draft.md:64-69`은 "core 하나라도 배포
  검증에 실패하면 제출 자체가 차단"이라고 명시한다.
- F-19의 `/developers`도 없다(`/llms.txt`만 존재).

**가점 상쇄:** 미구현을 **숨기지 않는다.** 배지를 "합성 샘플"에서 "구조화 상태 선택
데모"로 정정하고 `/llms.txt`가 미제공 항목을 명시한다(r6 I3). 루브릭 R4 0점 조건
"명세와 다름·에러 노출"에는 해당하지 않는다.

**올리려면:** ① Vercel 배포로 URL을 확보하고 README·명세에 기록(+2점 이상, G1 해소).
② F-01 합성 샘플 3건을 먼저 구현(가장 저비용 코어). ③ F-08 브리핑 PDF는 템플릿 조립이라
LLM 없이 가능 — W3 앞당기기 검토.

### R5 독창성·차별성 — 8/10 (가중치 10)

**근거:** 기존 서비스 비교표가 기관 자사 서비스를 포함해 존재한다 —
`docs/planning/10-proposal-draft.md` §4.4(시티즌코난·피싱아이즈·카카오뱅크·후스콜·
금융회사 대표채널·112·1394·1332·본인계좌 일괄지급정지·ECRM 10종). 기호 정의가 엄격하다
("`X`는 실제 기능 부재를 단정하지 않는다"). `docs/research/04-verification-digest.md`의
💀 전제(하나-2, 신한-2, 카뱅-2, 생보협-2)를 사용하지 않았고 "최초·유일" 표현 0건.
차별점(상태별 순서 + 한 번 입력한 사실 재사용 + 사실 수준별 사용자 확인 상태)이
구현으로 뒷받침되기 시작했다 — 상태 전환 비교 diff와 `ActionFactEvent[]` append-only
이력이 실제 화면에서 작동한다.

**감점:** 골든타임 자신의 비교표 행이 아직 `계획`이다. §4.4가 요구하는 승격 단계
(`계획` → `구현` → `배포 검증`) 중 `구현`으로 올릴 수 있는 항목이 생겼는데 반영되지 않았다.

**올리려면:** §4.4 비교표에서 현재 구현·검증된 항목(긴급 첫 행동, 사용자 확인 상태)을
`구현`으로 승격하고 근거(테스트·실측)를 각주로 붙인다.

### R6 기대효과·시상 정합성 — 6/10 (가중치 10)

**근거:** 특정 시상 기관 정책 과제와의 연결이 유지된다 — 금융위 범정부 TF의 "메신저·SNS
신종 스캠 풍선효과 후속 과제"(`docs/planning/10-proposal-draft.md` §3.2, 출처 확인됨)와
서비스의 소비자 대면 행동 오케스트레이션이 연결된다. 사회적 가치(고령자 모드·가족 대리인
`user_role`) 서사가 존재하고 실제 구현됐다.

**감점:** **수혜자 규모 정량화가 r5 검증으로 약화됐다.** "2025년 피해 1조 2,578억 원·
23,360건"이 기준 연도·집계 정의 부재로 금지되어, 현재 규모 근거는 "2025.10~2026.4
발생 9,353건·피해 4,936억 원(전년 동기 대비 35.3% 감소)"뿐이다. 감소 추세 수치는
문제의 시급성을 뒷받침하기 어렵다. 고령자 모드의 정량 근거(50대 이상 19.7%)도 금지되어
접근성 요구 충족으로 대체됐다(r5 H3).

**올리려면:** 기준 연도·집계 정의가 명시된 경찰청 공식 통계를 확보해 규모를 복원하거나,
"감소 추세 속 신종 스캠 전환"을 규모가 아닌 **구조 변화** 서사로 재구성해 정책 과제와의
정합성을 강화한다.

### R7 문서 품질·AI 판독성 — 9/10 (가중치 5)

**근거:** 계약 문서 체계가 추적 가능하다 — 개정 계약 r1~r6(`13`·`15`·`17`·`19`·`21`·`22`)
각각이 결정 번호(D/E/F/G/H/I)를 갖고, 기능명세서 말미 개정 이력이 전부를 참조한다.
핵심 주장이 표로 구조화됐고(§4.5.3 규칙 표, §4.5.4 인수 스냅샷, §5.4.1 지표 판정 규칙),
용어가 문서↔코드↔화면에서 일치한다("행동 이벤트 이력(내 기기 보관)"이 계약·구현·UI에
동일). 수치마다 출처 URL이 붙고 검증 결과가 별도 문서로 분리됐다.

**감점:** `docs/planning/`에 문서가 22개 누적되어 처음 보는 심사위원이 진입점을 찾기
어렵다. `README.md`가 현행화됐으나 개정 계약 6종의 관계도가 없다.

**올리려면:** `docs/planning/README.md`에 "지금 유효한 계약은 `11-spec-draft.md` 0.5-r6
하나이며 `13`~`22`는 그 개정 이력"이라는 1문단 안내와 관계도를 추가.

## 개선 백로그 TOP 5 (예상 점수 상승 순)

1. **[+4.0점 예상] Vercel 배포 + URL 기록** — `app/`을 Vercel에 배포하고 URL을
   `README.md`·`docs/planning/11-spec-draft.md`에 기록. **G1 FAIL → PASS**,
   R4 4→6점. 현재 외부 의존성이 0(모델 호출·DB 없음)이라 즉시 배포 가능한 상태다.
2. **[+2.0점 예상] F-01 합성 샘플 3건 구현** — 검수된 합성 메시지 3건 + `scenario_id`
   선택. LLM 없이 가능하며, 배지를 "합성 샘플"로 정직하게 승격할 수 있다. R4 +1,
   R1 +0.5(입력 다양성으로 AI 필요성이 드러남).
3. **[+2.0점 예상] W4 LLM 3단계 중 트리아지 1단계 선행 구현** — F-03 판정의 최소 경로만
   올려 화면에 AI 기여가 보이게 한다. R1 6→8, R3 7→8. 깊이 신호 D3 충족.
4. **[+1.5점 예상] 금감원 620명 설문 원문 확보 또는 대체 1차 통계 확보** — R2 7→8,
   R6 6→7. `app/docs/evidence-verified.md`가 "탐색했으나 미발견"으로 기록한 상태이며
   금감원 보도자료 아카이브·정책브리핑 재탐색이 필요하다.
5. **[+0.5점 예상] §4.4 비교표 골든타임 행을 `구현`으로 승격 + `docs/planning/README.md`
   계약 관계도** — R5 8→9, R7 9→10.

## 이전 리포트 대비

직전은 `008-P.md`(스테이지 P, 92.8/100). **스테이지가 P→M으로 바뀌어 적용 차원과 게이트가
달라졌으므로 총점 직접 비교는 무효다.** P는 R4를 "구현 계획의 구체성"으로 대체 평가했으나
M은 실물 작동을 본다.

차원별 비교(P에서도 채점한 차원):

| 차원 | 008-P | 009-M | 델타 | 해석 |
|---|---:|---:|---:|---|
| R1 | 9 | 6 | **−3** | 계획으로는 AI가 핵심이었으나 실물에 AI 경로 0건 |
| R2 | 9 | 7 | **−2** | 1차 출처 검증이 인용 4건을 기각 — 정직성↑ 근거강도↓ |
| R3 | 9 | 7 | **−2** | 설계 깊이는 유지되나 실구현은 D4·D5만 |
| R5 | 9 | 8 | −1 | 비교표 자기 행 승격 미반영 |
| R6 | 9 | 6 | **−3** | 규모 수치 금지로 정량화 약화 |
| R7 | 10 | 9 | −1 | 문서 누적으로 진입점 저하 |

**하락은 품질 저하가 아니라 평가 기준 전환의 결과다.** P는 "계획이 타당한가"를, M은
"실물이 작동하는가"를 본다. 다만 R2·R6 하락은 실질적이다 — 1차 출처 검증이 기존 근거를
기각했고 대체 근거를 아직 확보하지 못했다.

**P단계 백로그 이행 여부:** ① hwpx 양식 전사 리허설 — 미착수(W6). ② 경쟁 서비스 실기기
캡처 — 미착수(사용자 액션 대기). ③ 금감원 2021 설문 원문 링크 교체 — **착수했고 실패를
확인**해 해당 수치를 전면 금지 처리(r5 H3). 백로그 ③은 "해결"이 아니라 "불가 판정"으로
닫혔으며, 이것이 R2·R6 하락의 직접 원인이다.

## 3회 연속 미달 경고 해당 여부

M 스테이지 첫 채점이므로 해당 없음. 다만 **R4가 총점을 20점 가중치로 끌어내리고 있고,
그 원인의 대부분(배포 URL·코어 미구현)은 주차 일정상 W6·W4에 배정돼 있다.** 다음 M 채점
전에 백로그 1(배포)만 이행해도 63.5 → 67.5 수준이 되며, 백로그 1~3을 이행하면 목표 85에
근접한다.

## 루브릭 개정 제안

M 스테이지를 **주차별로 세분**할 것을 제안한다. 현재 루브릭은 M을 단일 스테이지로 보아
W1 시점에도 "배포 URL 즉시 접속"과 "코어 전부 작동"을 요구하므로, W1~W5 동안 R4가 구조적으로
낮게 나온다. `M-W1`(엔진·게이트) / `M-W4`(AI 파이프라인) / `M-W6`(배포·동결)로 나누고
주차별 목표 점수를 두면 진척 신호가 정확해진다. **본 리포트는 현행 루브릭 그대로 채점했다.**
