# 의사결정 로그

주요 결정을 시간순으로 기록한다. 형식: 날짜 / 결정 / 근거.

## 2026-07-24 — 참가 형태: 개인 참가

1인 + AI 에이전트 체제로 진행. Claude Code를 오케스트레이터로, Codex CLI를 하위 코딩
에이전트로 운용한다.

## 2026-07-24 — 주제 선정 방식: 후원사 현안 딥리서치 기반

세부 주제 예시에서 고르지 않고, 주최(금융보안원)·후원(금융위)·공동개최 5사가 실제로
풀고 싶어하는 문제를 리서치로 발굴해 sharp한 문제 인식으로 승부한다. 시상 구조상 특정
기관의 현안에 정확히 부합하는 주제가 유리하다고 판단.

## 2026-07-24 — Codex 서브에이전트 모델: gpt-5.6-sol + reasoning xhigh

초기 리서치·검증 에이전트가 기본 설정(gpt-5.6-luna, effort medium)으로 실행된 것을 확인,
품질 우선 정책으로 gpt-5.6-sol + xhigh를 명시하도록 변경. `~/.codex/config.toml` 기본값도
동일하게 설정됨. 검증 단계부터 sol로 재실행.

## 2026-07-24 — 리서치 검증 완료: 확정 수치만 사용 원칙

sol/xhigh 검증 에이전트 7개로 luna 리서치 397개 주장을 전수 대조 (66% 확인, 정정 다수).
이후 모든 제출물·문서는 `docs/research/04-verification-digest.md`의 정정표와 기관별
`05-verification.md` 판정을 통과한 수치만 인용한다. 후보 21개 중 4개 폐기(💀), 15개 재정의(🔧),
2개 생존(✅) — 생존 2개가 모두 "사기 탐지 이후 대화형 판단보조" 공백을 가리킴.

## 2026-07-24 — 주제 확정: "골든타임 — AI 사기대응 상황실"

부서 스케일 컨셉 4개 비교(`05-solution-concepts.md`) 후 C1 확정, 심사 체험 중심으로 담금질
(`06-decision.md`). 구조: 전면 데스크(전 국민이 오늘 쓸 수 있는 판정→골든타임 행동
오케스트레이션→신고·증거 산출물) + 후면 상황실(6개 에이전트 부서, 심사위원 자신의 사건이
라이브로 반영) + agent 인터페이스(API·MCP·llms.txt) + 공개 안전성 리포트. v1에서 다국어·
실연동·통화 실시간 분석·보험사기는 의도적 제외(v2).

## 2026-07-24 — 기술 스택 확정: TypeScript 단일 스택

Next.js(App Router)+TypeScript / Vercel 배포 / Claude API(판정·에이전트) / SSE 실시간 /
Neon Postgres / MCP TypeScript SDK / Tailwind+shadcn. 근거와 기각 대안은
`docs/planning/03-stack-proposal.md`. 핵심 근거: 심사 기간(9/7~9/11) 무중단을 관리형
인프라로 담보 + 1인 유지보수·Codex 위임 효율. 사용자가 P단계 ①~④ 일괄 진행을 위임하며 확정.

## 2026-07-24 — P단계 산출물 검증: two-way 리뷰 루프

기획서·기능명세서 초안은 ① judge 스킬(AI 루브릭 채점) ② 휴먼 심사위원 페르소나 리뷰(Codex
sol/xhigh)의 두 관점을 모두 통과할 때까지 개선 루프를 돈다. 데이콘 별도 양식 부재 확인 →
심사 기준 역산 구조로 작성.

## 2026-07-25 — 개정 r2 반영·검증: 조치 결정 엔진 총함수화 외 E1~E8

패널 2라운드 계약 E1~E8을 0.3-r2로 반영(judge 006-P: 92.2 PASS). 패널 3라운드가 1,152개
상태 조합 전수 검증으로 행 커버리지 완결을 확인하되, 결정적 출력 미달(unknown 우선순위
역전·초과 카드 필드 부재 등)로 **M단계 조건부 동의 + 진입 조건 4**를 판정
(`16-human-review-r3.md`).

## 2026-07-25 — 개정 r3(F1~F6): 코어 재동결·MCP 합성 전용·평가 합격선 고정

패널 3R 진입 조건을 닫는 확정 결정 (`17-revision-plan-r3.md`, 문서 0.4-r3 반영):

- **결정 엔진 결정성 완결**: 긴급 우선 불변식(잠재 severity 기반 질문 삽입 위치),
  `next_steps`·`purpose_slots` 필드, 전역 후속 독립 카드(접힘 금지), proxy·safe-device
  수정자 카드 규칙, 스냅샷 5종 알고리즘 재산출.
- **코어 재동결(이후 core 추가 금지)**: 런타임 코어 9(F-01·03·04·05·06·07·08·10·21) +
  정적 F-19 + **F-20은 core가 아닌 release gate로 재분류**(0.3-r2의 core 지정 철회) +
  showcase 1순위 F-13. 주차별 게이트 W1~W6.
- **MCP v1은 합성 `scenario_id` 전용**: 토큰 획득·주입 흐름 부재 상태에서 자유 텍스트를
  열지 않음. 토큰은 "보안 인증이 아닌 목적 고지 동의+비용 게이트"로 재정의, 발급 쿼터 수치화.
- **평가 합격선 고정**: 안전성 지표 9종 명칭 통일, 차단 판정은 점추정치 기준(구간은 병기),
  제품 KPI 5종 통과선 초기값 사전 등록.
- **행동 이벤트 원장 → "행동 이벤트 이력(로컬)" 개칭**: 감사 원장 아님 명시,
  `event_type`/`corrects_event_id` 정정 계약.

## 2026-07-25 — 개정 r4(G1~G7): M0 동결 마감 패치

패널 4라운드(`18-m0-freeze-verification.md`)가 조건 3(MCP·토큰)·4(코어 재동결) 충족,
조건 1·2 부분 충족(결함 V4-01~07)으로 판정 → `19-revision-plan-r4.md` G1~G7로 마감
(문서 0.5-r4):

- 전역 후속은 **승격 규칙**(중복 생성 금지), 강제 노출 카드의 슬롯 예약·낮은 severity부터
  축출, `priority`는 전체 순위 연속 번호(`number`), `official_sources` 배열화.
- 우선순위 문구 단일화(§4.5.1 우선 명시) + 확인 severity 부재 시 rank 7 기준.
- 제품 KPI 경계 완결: p95 최근접 순위 산식·N, KPI ③ 분모=홀드아웃 40, KPI ④ 쌍체
  비교·기준 0 처리, KPI ⑤ 부분집합 판정·하위 지표는 공개 전용.
- 스냅샷 기본값 `transfer_state=not_sent` 명기, "원장" 잔존 표현 소탕.

**M0 동결 상태**: 패널 4R이 "이 두 묶음이 닫히면 조건 3·4를 다시 열지 않고 동결 선언
가능"이라 판정 — r4 반영분에 대한 최종 확인 검증 후 동결 선언 예정.
