# AGENTS.md — 에이전트 공용 컨텍스트

이 파일은 이 저장소에서 작업하는 모든 코딩 에이전트(Codex 등)가 읽는 공통 컨텍스트다.

## 프로젝트

2026 금융 AI Challenge 공모전 참가 프로젝트. AI 기반 금융 현안 해결 웹서비스(MVP)를 개발해
2026. 9. 7. 10:00까지 배포 URL과 함께 제출한다. 배포된 URL은 9. 11.까지 무중단 유지되어야 한다.

- 주제: **골든타임(GoldenTime) — AI 사기대응 상황실.** 송금·앱 설치·인증정보 노출 직후의
  금융소비자에게 AI 판정보다 먼저 공식 첫 행동을 제시하고, 상태별 행동 카드·고정 문구·
  신고 준비 브리핑을 제공하는 무로그인 웹 서비스 (`docs/research/06-decision.md`)
- 기술 스택: **Next.js(App Router)+TypeScript / Vercel / Claude API / SSE /
  Neon Postgres / MCP TypeScript SDK / Tailwind+shadcn** (`docs/planning/03-stack-proposal.md`)
- 구현 계약: **`docs/planning/11-spec-draft.md`(기능명세서)가 단일 진실이다.** 기능
  ID(F-XX), 스키마, 조치 결정 엔진(§4.5), 인수 기준(§9)을 임의 해석·변경하지 않는다.
- 주요 결정 로그: `docs/decisions.md`

## 범위 계약 (2026-07-25 재동결 — 코어 추가·축소 금지)

- **런타임 코어 9 (삭제 불가):** F-01, F-03, F-04, F-05, F-06, F-07, F-08, F-10, F-21
- **정적 제출물:** F-19 (`llms.txt`·개발자 문서)
- **release gate:** F-20 안전성 리포트 (제출 전 예비 평가 게이트)
- **showcase (코어 E2E 통과 후 활성):** 1순위 F-13, 2순위 F-16·F-18(MCP는 v1 합성
  `scenario_id` 전용), 3순위 F-12·F-15
- **nice:** F-02, F-09, F-11 / **제거(재사용 금지):** F-14, F-17

## 디렉토리 구조

- `docs/competition/` — 공모전 요강, 제출물 체크리스트 (수정 금지, 참조용)
- `docs/research/` — 주제 발굴 리서치 산출물 (검증 다이제스트: `04-verification-digest.md`)
- `docs/planning/` — 기획서(`10`)·기능명세서(`11`)·개정 계약(`13`·`15`·`17`)·패널 리뷰(`12`·`14`·`16`)
- `app/` — MVP 웹서비스 코드 (M단계에서 생성)

## 작업 규칙

- 지시받은 작업 범위를 벗어난 파일을 수정하지 않는다.
- `docs/competition/` 아래 파일은 절대 수정하지 않는다.
- 실제 금융데이터·개인정보를 코드나 문서에 포함하지 않는다. 샘플 데이터는 합성 데이터만
  사용 (`is_synthetic=true`, 더미 번호 `010-0000-0000`, URL은 `.invalid`/`example.com`).
- 외부 코드·자료를 가져올 때는 라이선스를 확인하고 출처를 남긴다 (공모전 표절 규정상 실격 사유).
- 커밋은 오케스트레이터(Claude)가 수행한다. 에이전트는 파일 변경까지만 수행.

## 문서·용어 규칙 (심사 대응 — 위반 시 감점·실격 위험)

- **수치 인용은 검증 통과분만**: `docs/research/04-verification-digest.md` 정정표와
  `docs/planning/01-golden-time-sources.md`의 확정 표기만 사용하고 출처 링크를 병기한다.
- **금지 표현**: 피해 "폭증"(정부 공식 프레임은 감소+풍선효과), "최초·유일", 경쟁 서비스
  기능 부재 단정(`X`는 "검토한 공식 자료에서 확인되지 않음"의 의미), 미실측 성능 수치.
- **용어 계약**: "행동 이벤트 이력(내 기기 보관)"(감사 원장 아님) / "6단계 처리·검증
  파이프라인"(LLM 3단계+결정적 규칙 2단계+템플릿 1단계 — "6개 에이전트" 표현 금지) /
  `evidence_strength`=근거 충분도(확률·정확도 아님) / 안전성 결과는 "예비 평가" /
  긴급 행동·기관 연락처·법적 문구는 LLM 자유 생성 금지(고정 템플릿+슬롯).
- **개인정보 경계**: 사용자 사건은 서버 무저장(`sessionStorage` 전용), 개인 부속면 4종
  필드는 브라우저 메모리 전용(서버·모델·세션 전송 금지), 요청 본문 로깅 금지.
