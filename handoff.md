# 세션 인수인계 — M단계 W1 착수 (2026-07-27)

기준 커밋: **`bc0c5b2`** (origin/main 동기화 완료) / 제출까지 **41일** (2026-09-07 10:00)

---

## 0. 30초 요약

- 컨셉이 **2026-07-26에 전면 교체**됐다. 「골든타임」 → **「기록으로」 — 내 편의 사고조사실**
- **P단계 종료. judge 93.8/100** (목표 85). 기획서 `1.2` · 기능명세서 `1.0` 완성
- **다음 할 일은 W1 구현**이다 (§4). 문서 작업은 hwpx 전사 말고 남은 게 없다
- ⚠️ **`app/src/`는 아직 골든타임 빌드다.** 「기록으로」 기능은 **0개 작동**한다

---

## 1. 컨셉 — 한 문장

> 무단이체·명의도용·사기 피해자가, 은행이 **기억으로 답하라고 요구하는 항목**을 자기 기기에
> 남아 있는 **기록으로** 답하고, **어느 제도의 문이 열려 있는지와 그 문들 사이의 순서**를
> 신청 전에 확인하는 무설치·무로그인 웹 서비스.

### 문제 정의 (단일 출처·단일 기간·단일 모집단)

5대 은행, 2024-01~2025-08, 이인영 의원실이 금감원에서 제출받은 자료(2025-10-10 국정감사):

| 상담 | 신청 | 심사 제외 | 배상 |
|---:|---:|---:|---:|
| **2,135** | **173** (8.1%) | **60** (신청의 34.7%) | **18** — **상담 대비 0.84%** |

- **두 관문**: ① 요건(제외사유) ② 과실(전자금융거래법 시행령 §8 4개 호)
- **최대 손실 구간은 「상담→신청」 91.9%** — 단 그 사유 분포는 자료가 없어 `[추론]` 표기
- **34.7%는 하나의 덩어리가 아니다** — 통신사기피해환급법 §2②의 **단서 한 줄**이 갈림길이다.
  「피해자 직접 이체」는 그 법 **§2②가목의 정면 대상**, 「중고거래·게임아이템」은 **단서로 제외**
- **순서 제약**: 소송·압류를 먼저 걸면 **환급 경로가 닫힌다**(§4의2·§5①).
  어느 안내에도 없는 규칙이고, **이게 데모의 심장(20~24초)**이다

---

## 2. 읽는 순서 (진입점)

| # | 문서 | 왜 |
|:-:|---|---|
| 1 | `CLAUDE.md` · `AGENTS.md` | 자동 로드분. 설계 불변식·금지 표현이 여기 있다 |
| 2 | **`docs/planning/41-spec-draft.md`** | **구현 계약이자 단일 진실.** W1은 이 문서만 보면 된다 |
| 3 | `docs/planning/32-alt-route-track.md` | **R-02 규칙의 원본.** 조문 전문·함정 5개·기한 시계 |
| 4 | `docs/planning/40-proposal-draft.md` | 기획서 `1.2`. 화면·서사의 근거 |
| 5 | `docs/planning/31-solution-lock.md` | 컨셉 확정 근거(축 이동·A-면·D2) |
| 6 | `docs/judging/scores/012-P.md` | 최신 채점 + 남은 백로그 |

리서치 계보가 궁금하면 `docs/research/README.md`(3라운드 계보표)부터.
**`docs/archive/goldentime/`는 폐기분이다. 열지 않아도 되고, 인용하면 감점(−3)이다.**

---

## 3. 재사용 자산 — 무엇을 옮기고 무엇을 버리는가

`app/src/lib/decision/` (총 2,276줄) — **구조는 그대로, 데이터는 전부 교체**

| 파일 | 처리 |
|---|---|
| `engine.ts` (533줄) | **구조 이전.** 6단계(정규화→매칭→병합→출처합집합→우선순위) + fail-closed 검증. **4단계 「경로 의존 해소」를 신규 삽입**(명세서 §4.1) |
| `rules.ts` (358줄) | **전량 교체.** R-01~R-04로 다시 씀 (~67 규칙) |
| `__tests__/engine.exhaustive.test.ts` (354줄) | **하네스 이전.** 골든타임은 1,152조합 전수. 상태 조합 정의만 `CaseState` 기준으로 교체 |
| `__tests__/source-entailment*.ts` | **이전.** 인용 강제 검사에 그대로 쓴다 |
| `__tests__/registry·validation·serialization` | 이전 |

**컴포넌트 13개 중 재사용 후보**: `trust-notice` · `official-link` · `event-history` ·
`prohibition-block` · `scenario-picker`. 나머지(`rule0-desk`·`emergency-question`·
`action-card`·`copy-script`·`next-steps`)는 골든타임 전용이라 **삭제 대상**.

`app/src/lib/` 하위 `events`·`security`·`session`·`templates`·`accessibility`·`perf`는
도메인 무관이라 대체로 살아 있다. `comparison`·`scenarios`는 도메인 교체 필요.

**검증 명령**: `cd app && npm run verify` (typecheck → lint → test → build)

---

## 4. W1 작업 (07-27 ~ 08-02) — 이번 세션이 할 일

명세서 §9.1 W1 게이트:

1. **`CaseState` 확정** (명세서 §4.2) — 10개 필드.
   ⚠️ **금액은 `band`로만 받는다**(`account_balance_band`·`claim_amount_band`).
   원금액 필드를 만들면 금액 산출이 가능해져 불변식 위반이다.
   `confidence_label`은 `stated`/`recorded`/`absent` 3값 — **확률이 아니다**
2. **`engine.ts` 이식 + 4단계 「경로 의존 해소」 신규 구현**
3. **R-02 경로 배정 규칙 코드화 (~25개)** — 이번 주의 핵심.
   원본은 `32-alt-route-track.md` §1(분기표)·§2(기한 시계)·§3(함정 5개)·§6.2(규칙 표)
4. **합성 시나리오 스키마** — 쌍둥이 사건(같은 피해액, 다른 이체 주체) 포함.
   `is_synthetic=true` · `010-0000-0000` · `.invalid`
5. **전수 property 테스트 통과**

**규칙 형태 계약**: `{id, match, actions, citations[], effective_date}`.
**`citations`가 비면 빌드 실패**여야 한다(F-19 게이트 2번).

---

## 5. 절대 어기면 안 되는 것 (릴리스 차단 사유)

1. **판정은 생성형 AI 밖.** 관문 판정·경로 배정·과실 평가는 결정적 규칙 엔진. 같은 입력 → 같은 출력
2. **확률·승산·예상 금액을 출력하지 않는다.** **응답 스키마에 필드를 만들지 마라**
3. **제출·쓰기 엔드포인트를 만들지 않는다.** 에이전트가 대신 신청할 수 없어야 한다
4. **모든 규범 주장에 조문·공개자료 인용 강제**
5. **개인 사건 데이터 서버 무저장 · 요청 본문 무로깅**
6. **합성 사건 경로(F-01~F-08, F-10)는 모델 호출 없이 작동**해야 한다.
   AI 의존 구간은 **F-09·F-11뿐**이다 (첫 30초는 모델 비의존 = 무중단 담보)
7. **"AI가 판정한다·결정한다" 서술 금지** — AI 가이드라인 제3원칙 보조수단성
8. **대화형 챗봇 만들지 않는다** — 공동개최사가 이미 점유(누적 500만 명)

---

## 6. 오늘 밟은 함정 — 반복하지 마라

| 함정 | 사실 |
|---|---|
| **`law.go.kr` 웹페이지** | SPA라 **조문 본문이 안 나온다.** 제목만 반환됨. → **법제처 Open API를 써라**: `https://www.law.go.kr/DRF/lawService.do?OC=test&target=law&MST=<번호>&type=XML` (검색은 `lawSearch.do?...&query=<법령명>`). `OC=test`로 동작 확인함 |
| **법 문언 ≠ 시행령** | 통신사기피해환급법 §5①6호 본문은 **"3만원"**인데 **시행령 §6②이 정한 금액은 1만원**이다. 법만 읽고 인용하면 오류. **인용 시 시행일 병기 필수** |
| **「176건」** | **존재하지 않는 수치.** 원본은 **173건** |
| **「우리·하나 0원」** | 사실이지만 **대외 문서에 특정 은행 지목 금지**(공동개최사). 은행권 전체값만 쓴다 |
| **「무과실 배상 통과」** | **계류 중**이다. 통과한 의안 2216766은 **가상자산 건**이라 무과실·한도 조항이 없다 |
| **DuckDuckGo HTML 스크래핑** | **CAPTCHA로 차단**된다. 우회 금지 |

### 확보한 법령 MST 번호 (재조회 불필요)

| 법령 | MST | 시행일 |
|---|---|---|
| 통신사기피해환급법 | `260863` | 2024-08-28 |
| 같은 법 시행령 | `279543` | 2026-05-12 |
| 금융소비자보호법 | `277247` | 2026-01-02 |
| 같은 법 시행령 | `285715` | 2026-04-28 |
| 소액사건심판규칙 | `187995` | 2017-01-01 |

---

## 7. 도구 상태 ⚠️

| 도구 | 상태 |
|---|---|
| **WebSearch** | **예산 소진 (200/200).** 새 세션에서 리셋되는지 먼저 확인할 것 |
| **WebFetch** | 사용 가능 (별도 예산) |
| **Codex CLI** | **2026-07-30까지 쿼터 소진.** 그 전까지는 Claude가 직접 구현하거나 서브에이전트 사용 |
| 법제처 Open API | 정상 (`curl` + `OC=test`) |

---

## 8. 미해결 과제

| # | 과제 | 영향 |
|:-:|---|---|
| 1 | **카카오뱅크 소비자보호 > 비대면 금융사고(FA) 페이지 본문** — SPA로 미취득(`curl`·Googlebot UA·WebFetch 3종 실패) | **판정이 뒤집힐 수 있다.** 이미 앱에서 증적·심사 안내를 제공하면 G-기존수단 FAIL. 모바일 앱 실물 확인 필요 |
| 2 | **로맨스스캠의 통신사기피해환급법 §2②가목 해당 여부** | `[해석]` 상태. 화면은 **확정 판정 없이 양쪽 논거 제시**로 설계됨(그대로 두면 됨) |
| 3 | **hwpx 양식 전사** — `docs/planning/(첨부1)`·`(첨부2)` | G2 게이트. W6 작업 |
| 4 | **팀명·구성원** — `[팀명]`·`[성명]` 플레이스홀더 | **사용자 기입 필요** |
| 5 | 배포 URL 무로그인 접근 재확인 | G7. 현재 배포본은 골든타임 빌드 |

### judge 012 백로그 (점수 상승 순)

1. hwpx 전사 + 팀명 (게이트)
2. **[+0.6] (A) 30초 경로에 AI 산출물 출처 배지** — 30초만 보는 심사위원에게도 AI가 보이게
3. **[+0.4] R6 결과 지표 대리값 1개** — 홀드아웃 40건으로 "관문 ① 판정과 실제 제도 요건의 일치율"

---

## 9. 채점 상태

| 리포트 | 점수 | 비고 |
|---|:-:|---|
| 010-P | 82.5 | 컨셉 교체 후 첫 채점 |
| 011-P | 91.9 | 백로그 5건 이행 |
| **012-P** | **93.8** | 대체 경로 트랙 + 명세서. **목표 85 초과** |

> ⚠️ **M단계 첫 채점은 급락한다.** M 루브릭은 R4(가중치 20)를 **배포본 실측**으로 재고,
> D 신호도 *"배포본에서 작동하는가"*로 센다. 골든타임 때 P 92.8 → M 63.5였다.
> **문서 93.8은 M 점수를 예측하지 않는다. 놀라지 마라.**

커밋할 때마다 PostToolUse 훅이 채점을 리마인드한다. `docs/archive/`는 채점 대상이 아니다.

---

## 10. Git

- 원격 `https://github.com/mamekuma-sys/finance-ai-challenge`, 브랜치 `main`
- **커밋 후 즉시 `git push origin main`** — 로컬 전용 커밋 금지 (세션·기기 간 인계 워크플로우)
- 골든타임 스냅샷: 태그 **`goldentime-final`** (`9fdfc68`), 원격 반영 완료
