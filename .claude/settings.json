{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command // \"\"' | grep -q \"git commit\" && echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PostToolUse\",\"additionalContext\":\"[judge-hook] 커밋이 감지되었습니다. 스테이지 산출물 커밋이라면 docs/judging/rubric.md 기준으로 judge 스킬을 실행해 채점하고, 목표 미달 시 개선 백로그를 처리하세요.\"},\"suppressOutput\":true}' || true",
            "statusMessage": "judge 채점 리마인드 확인 중"
          }
        ]
      }
    ]
  }
}
